version: '3.8'

services:
  app:
    image: it-da:latest
    restart: always
    env_file:
      - .env
    environment:
      - MONGO_URI=mongodb://host.docker.internal:27017/bravo
    ports:
      - "3001:3001"
    extra_hosts:
      - "host.docker.internal:host-gateway"